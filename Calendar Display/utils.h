#include <string>
void renderCalendar(int month, int startDay, int year);
bool isLeapYear(int year);
int *getInput();
int getNumOfDaysInMonth(int month, int year);
int getStartDay(int month, int year);
int getTotalNumOfDays(int month, int day, int year);
